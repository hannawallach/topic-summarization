associative memory; memory; storage distributed content addressable memories; sparse interconnectivity robustness memory storage; degradation memory recall increased storage; distributed format information storage carries; outer product patterns anderson discussed; errors sequence stored datum pairs; distributed memory sdm associative memory; kanerva sparse distributed memory sdm
